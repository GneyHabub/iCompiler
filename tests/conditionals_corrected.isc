routine submain(a : integer , b : integer) : integer is
	if a = b then
		print(0)
	else
		if a > b then
			print(1)
		else
			print(-1)
		end
	end
	return 0;
end

routine main(): integer is
	return submain(1, 1);
end
